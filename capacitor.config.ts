import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kronos.webapp',
  appName: 'KronosWebApp',
  webDir: '.',
  bundledWebRuntime: false
};

export default config;
